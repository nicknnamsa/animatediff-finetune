# AnimateDiff Fine-Tune

Fine-tuning AnimateDiff's motion module on [your chosen domain] to generate short text-to-video clips.

## Status
🚧 In progress — Day 1: environment setup

## Setup
See requirements.txt. Requires Python 3.11+.

## Structure
- `data/` — training clips and prompts
- `scripts/` — training and inference scripts
- `notebooks/` — exploration and testing
- `outputs/` — generated samples